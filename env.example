# ============================================
# NEXT.JS PUBLIC ENVIRONMENT VARIABLES
# ============================================
# Variables dengan prefix NEXT_PUBLIC_ akan diexpose ke browser
# Jangan simpan sensitive data di sini!

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# Backend API URL
# For local development: http://localhost:5000
# For production: https://your-backend-domain.com
# This variable is accessible in browser code (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================
# NextAuth Secret Key (REQUIRED!)
# Generate a strong random string (min 32 characters)
# Example: openssl rand -base64 32
# This must match or be securely generated for production
NEXTAUTH_SECRET=your-nextauth-secret-key-change-in-production-min-32-chars

# NextAuth URL
# For local development: http://localhost:3000
# For production: https://your-frontend-domain.com
NEXTAUTH_URL=http://localhost:3000

# ============================================
# NOTES:
# ============================================
# 1. Copy this file to .env.local: cp env.example .env.local
# 2. Update all values, especially NEXTAUTH_SECRET
# 3. Never commit .env.local or .env files to version control
# 4. For production, use strong random strings for NEXTAUTH_SECRET
# 5. NEXTAUTH_SECRET should be different from backend JWT_SECRET
# 6. Next.js automatically loads .env.local file
# 7. NEXT_PUBLIC_* variables are accessible in browser code
# 8. Variables without NEXT_PUBLIC_ are server-side only

